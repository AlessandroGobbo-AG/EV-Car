'''
    if st.session_state.user_state['logged_in']:
        pages = {
            "Report":[
                st.Page("PAGES/dashboard.py", title='Dashboard'),
                st.Page("PAGES/admin.py", title='Admin'),
                st.Page("PAGES/sale.py", title='Sale')
            ]
        }
        pg = st.navigation(pages)
        pg.run()
        
        # Pulsante logout
        if logout(): 
            st.session_state.clear()
            st.rerun()
    '''